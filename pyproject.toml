[project]
name = "best_available_fantasy_football"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "beautifulsoup4>=4.12",
    "lxml>=5",
    "matplotlib>=3.8",
    "numpy>=1.26",
    "pandas>=2.2",
]

[tool.uv]
package = false
